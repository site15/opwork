# TODO по проекту OpWork (с контекстом)

## 1. Авторизация и регистрация

### 1.1 Экран авторизации
Контекст: страница входа (`{22B6D708-3FD4-4865-AC9B-5171DC5F8DD9}.png`).

- Обновить иконку/брендовый знак.
- Обновить блок копирайта.
- Убрать возможность темизации интерфейса на этом экране.
- Отключить темную тему.
- Убрать китайскую локализацию.
- Обновить заголовок и описание страницы.

### 1.2 Экран регистрации
Контекст: страница регистрации (`{B045021E-BAA1-4059-AC52-09422F0E6CF0}.png`).

- Удалить блок с согласием на политику или заменить его на финальный юридический текст.
- Перенести валидацию согласия на бэкенд.

### 1.3 Поведение после регистрации/входа
Контекст: текущий редирект ведет на моковую аналитику (`{BEF7D9F7-E9FB-4471-B351-094E80A0C6D6}.png`, `{5F7A0769-F8E8-4EF5-97E3-3CB66E8B993D}.png`).

- Убрать/скрыть моковую панель управления.
- Для компании: если профиль не заполнен, вести на `Моя компания`; если профиль заполнен и вакансий нет, вести на создание вакансии; если вакансии есть, вести на `Мои вакансии`.
- Для соискателя: если профиль не заполнен, вести на профиль; если резюме не заполнено, вести на `Мое резюме`; иначе вести на `Мои отклики`.

## 2. Настройки интерфейса

Контекст: панель настроек интерфейса (`{F01E31E8-3C7A-4146-95BA-60F387DECCA5}.png`, `{DE53BB7A-B675-4C23-BCA6-3B2B7F5F6DF9}.png`).

- Отключить боковую панель кастомизации.
- Убрать настройку часового пояса.
- Убрать кнопку полноэкранного режима.

## 3. Вакансии компании

### 3.1 Форма вакансии
Контекст: создание/редактирование вакансии (`{EF1D8C9F-D7BA-4447-988F-0B28489D527A}.png`).

- Запретить ручное редактирование поля статуса.
- Добавить отдельные кнопки для смены статуса.
- Установить статус по умолчанию: `DRAFT`.
- Автоматически выставлять дату публикации при переходе в `ACTIVE`.
- Добавить третий вариант для удаленной работы: `Любой`.
- Поле валюты зарплаты перевести в селект со списком валют.
- Сделать поля `уровень опыта`, `требования`, `обязанности` опциональными в базе и интерфейсе.

### 3.2 Навигация после создания вакансии
Контекст: после создания открывается просмотр (`{6A4AB7C3-3A5F-4006-BB1B-9C1F5DCF6FA6}.png`).

- После создания вакансии вести пользователя на страницу редактирования, а не просмотра.

### 3.3 Теги вакансии
Контекст: таблица тегов и форма тега (`{B452DF7E-FFE5-4EBD-8EC0-0D92A5CAC80C}.png`, `{7857441A-7D53-4FB9-BD7A-0DB71A1EC698}.png`).

- Добавить тип контрола выбора цвета в генераторе форм.
- Адаптировать кастомные формы под новый контрол выбора цвета.

### 3.4 Удаление вакансии
Контекст: модальное окно удаления (`{4C069865-7469-4B5F-B3D3-468BF007A20C}.png`, `{15BC6B8A-325B-494F-84B8-1E7A0EA9FC74}.png`).

- После успешного удаления вакансии выполнять редирект на список вакансий.

### 3.5 Список вакансий и фильтры
Контекст: список и фильтры (`{2D276736-D7EA-49B8-99E6-B9E2F9E91C4C}.png`, `vacny_filter_panel.png`).

- Переименовать кнопку `Добавить` в `Добавить вакансию`.
- Переименовать заголовок `Фильтр` в `Фильтры вакансий`.
- Переименовать кнопку `Найти` в `Найти вакансию`.

## 4. Заявки компании

### 4.1 История статусов
Контекст: карточка и форма смены статуса (`vacancy-application-status.png`, `vacany-application-notes.png`, `vacancy-application-notes-in-row.png`).

- Добавить хранение истории статусов и примечаний (кто, когда, что изменил).
- Добавить кнопку просмотра истории рядом с текущим статусом/примечанием.

### 4.2 Фильтры заявок
Контекст: панель фильтров заявок (`vacancy-application-filter.png`).

- Переименовать заголовок в `Фильтры заявок`.
- Переименовать кнопку в `Найти заявку`.

## 5. Резюме соискателя

### 5.1 Образование
Контекст: форма образования (`{DF9FF344-7B21-41FA-A748-988CF98645D7}.png`).

- Упростить дату обучения: оставить только год (без месяца и дня).

### 5.2 Опыт работы
Контекст: форма опыта (`{C2FB245D-0B44-4FD1-8A2D-431A791B721C}.png`).

- Упростить дату работы: оставить только год (без месяца и дня).

### 5.3 Навыки
Контекст: форма навыка (`{6B01D19F-4377-4D0E-8F1F-5A1FD2B398FE}.png`).

- Удалить или пересмотреть поле `последний раз использовался`.

## 6. Мои отклики соискателя

Контекст: панель фильтров в разделе откликов (`{3C209803-9456-443F-A1D9-0AD6B4663CD9}.png`).

- Переименовать `Фильтры` в `Фильтрация откликов`.
- Переименовать кнопку `Найти` в `Найти отклики`.

## 7. Нотификации

### 7.1 Визуальные элементы и действия
Контекст: выпадающий список уведомлений (`{23B67EE9-0C51-4616-A7C1-563414F432DF}.png`).

- Исправить или удалить неотображающиеся иконки в элементах уведомлений.
- Исправить кнопку `Посмотреть все сообщения` (должна открывать полный список уведомлений, включая просмотренные и архивные).

### 7.2 События нотификаций
Контекст: бизнес-логика уведомлений.

- Реализовать уведомление соискателю при изменении статуса заявки компанией.
- Реализовать уведомление соискателю при добавлении примечания к заявке компанией.

## 8. Будущие разделы (нужны скриншоты)

### 8.1 Переписка
Контекст: чат между соискателем и компанией.

- Добавить скриншот чата после реализации.
- Путь-заглушка: `TODO-add-chat-screenshot.png`.

### 8.2 Приглашения от компании
Контекст: список приглашений и детальная карточка.

- Добавить скриншот списка приглашений после реализации.
- Путь-заглушка: `TODO-add-invitations-list-screenshot.png`.
- Добавить скриншот детальной карточки приглашения после реализации.
- Путь-заглушка: `TODO-add-invitation-detail-screenshot.png`.

### 8.3 Дашборд
Контекст: аналитические панели для соискателя и компании.

- Добавить скриншот дашборда соискателя после реализации.
- Путь-заглушка: `TODO-add-job-seeker-dashboard-screenshot.png`.
- Добавить скриншот дашборда компании после реализации.
- Путь-заглушка: `TODO-add-employer-dashboard-screenshot.png`.
